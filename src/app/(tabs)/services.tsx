import { Text, View } from "react-native";

export default function ServicesScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-gray-400">Dịch vụ (chờ BE)</Text>
    </View>
  );
}
